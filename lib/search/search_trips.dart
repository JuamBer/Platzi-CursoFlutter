import 'package:flutter/material.dart';


class SearchTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    var searchTrips = Container(
      color: Colors.amber,
    );

    return searchTrips;
  }
}